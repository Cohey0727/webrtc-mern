import { Styles } from "@/utils";

const styles = {
  root: {
    height: "48px",
    flex: "0 0 auto",
    padding: "0 16px",
    justifyContent: "space-between",
    alignItems: "center",
  },
} satisfies Styles;

export default styles;
