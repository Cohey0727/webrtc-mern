import { Styles } from "@/utils";

const styles = {
  root: {
    flex: "1 1 auto",
    overflow: "hidden",
  },
  directionColumn: {
    flexDirection: "column",
  },
} satisfies Styles;

export default styles;
